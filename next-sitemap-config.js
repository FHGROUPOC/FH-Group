module.exports = {
  siteUrl: 'https://www.fhgroupoc.com/', // Replace with your site's URL
  generateRobotsTxt: true, // Optional: Generates robots.txt
  changefreq: 'daily', // Set the frequency of updates (can be 'hourly', 'daily', etc.)
  priority: 0.7, // Set the priority of pages (between 0.0 and 1.0)
  sitemapSize: 5000, // Default is 5000 URLs per sitemap file
};